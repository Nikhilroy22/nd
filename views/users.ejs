<%- include('header') %>
<h1 >📋 All Users</h1>

      <% users.forEach(user => { %>
      
           <h1 style="color:green"> <%= user.name %></h1>
          
   
          <% if (user.token) { %>
  <p style="color:red"><%= user.token %></p>
<% } %>
      <% }) %>
   
<%- include('footer') %>